# 环境变量配置

# 后端 API 地址
# - 开发环境：推荐保持 /api，通过 Vite 代理到本机后端（默认 http://localhost:8000）
# 生产环境部署时需要设置实际的后端地址
VITE_API_URL=/api

# 开发环境可选：修改 Vite dev proxy 的后端目标地址
# VITE_PROXY_TARGET=http://localhost:8000

# API 密钥 (如果后端设置了 STOCK_SCREENER_API_KEY)
# VITE_API_KEY=your-api-key-here
